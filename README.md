# LongTermInvestments
A project to analyze sec financial datasets and find potential investment strategies.

To start make a virtual python environment using

```
python -m venv venv #creates virtual environment named venv
source venv/bin/activate
pip install -r requirements.txt
```

run the updateDataset.py file to download any new data